export type Todos = {
  id: string;
  title: string;
  contents: string;
  isDone: boolean;
};
export type TodoData = {
  title: string;
  contents: string;
};

export type companyInfo = {
  name: string;
  description: string;
  image: string;
};
