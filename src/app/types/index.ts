export type Todos = {
  id: string;
  title: string;
  contents: string;
  isDone: boolean;
};
export type TodoData = {
  title: string;
  contents: string;
  isDone: boolean;
};

export type newTodo = {
  title: string;
  contents: string;
};

export type companyInfo = {
  name: string;
  description: string;
  image: string;
};

export type TodoProps = {
  isActive: boolean;
};

export type params = {
  id: string;
};
